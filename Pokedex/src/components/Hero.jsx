import React from 'react'

function Hero() {

  return (
    <div className='w-full h-200vh '>
      console.log(pokemonData)
        {/* {pokemonData.map((elem,index) => {
          return <Pokecard key={index} pokemonData = {elem} />
        })} */}
    </div>
  )
}

export default Hero
